language: python

python:
  - '2.7'


before_install:
  - sudo ./install -i
  - sudo ./install -b
  - sudo ./install -r
  - sudo ./install -m
  - sudo ./install

before-script:
  - bin/dpm-start
  - sleep 10

script:
  - tests/tests-dpm

